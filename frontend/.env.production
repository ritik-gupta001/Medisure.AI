# Production API URL - You'll need to replace this with your deployed backend URL
# For now, this points to localhost for development
REACT_APP_API_URL=http://localhost:8000

# You can override this in Netlify's environment variables section
# with your actual deployed backend URL